/**
 * Reusable UI components for extensions
 *
 * Note: UI components should be provided by the host application via PluginContext,
 * not directly exported from the SDK. This keeps the SDK as a pure contract layer.
 */

// No components exported - extensions receive components via context.ui.components
